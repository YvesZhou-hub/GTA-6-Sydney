# v0.1.8 evidence

Four native suites (189 automated checks) are bound to the same exported App as the seven 1080p trailer captures. See build.json, *-launch.json and trailer-capture.json. The archive is audited separately after public-source packaging. Screenshots are copied without editing. Text paths are normalized; the index retains original and published SHA256 values. Automated inputs and fixed-step recordings do not establish human hardware testing, real-time FPS, whole-region 1:1 accuracy, or promotion reach.
