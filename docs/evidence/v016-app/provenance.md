# v0.1.6 evidence

`build.json` and the three native launch/report/log sets refer to the final exported App whose SHA256 is in that manifest. Local paths are normalized; numerical results and hashes are unchanged. Images under `docs/screenshots/v016/` are selected direct GPU captures, without editing. `screenshots.json` lists their hashes and visual review notes; the native report records all capture attempts.

Files with `source` in the name are isolated source fixtures, separately labelled and not counted as exported-App checks. Prior-version evidence folders remain historical. No player save, private runtime settings, real-world photograph or externally downloaded texture is included. QA saves are isolated and retained.
